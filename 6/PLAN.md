# Plan

Phased implementation checklist for `6.p8`, based on [README.md](README.md) and [DESIGN.md](DESIGN.md). A few design decisions are still open (see README's Open Questions); resolve the noise-generation approach in particular before starting Phase 1, since it shapes everything downstream. Given this game's size, expect the token budget (Phase 9) to force simplification somewhere, don't treat DESIGN.md's first-pass tuning numbers or even the full 16/8/6 content counts as guaranteed to survive unchanged.

## Phase 0: Shared lib/ prerequisites

CLAUDE.md and this plan both assumed `lib/` already covers input, state machine, tweening, collision, HUD, map queries, and seeded RNG. It doesn't: only `screen.lua`, `title.lua`, and `math.lua` exist (see [`../lib/CLAUDE.md`](../lib/CLAUDE.md) and [`../lib/PLAN.md`](../lib/PLAN.md)). Game 6 needs exactly the missing ones, and this is the most token-tight game in the jam, so build these first rather than discovering the cost mid-Phase-1.

- [x] `lib/state.lua`: `goto_state(ns)`/`return_state()` (`gs`/`gsret` pair), covering the "resume whichever state opened this overlay" need for the Help screen (openable from Playing or Inventory — game 2's `help_on` precedent doesn't need this since it only ever has one caller state). Deliberately not a full dispatch-table FSM; games 1/2/3/5's plain `if gs==n then` chains stay the pattern for the 5-scene main dispatch, since a generic table-of-functions dispatcher costs more tokens per call than that for this few states. See the file's own header comment.
- [x] `lib/collision.lua`: `rect_hit(x1,y1,w1,h1,x2,y2,w2,h2)` AABB overlap check, for player-block, player-monster, player-hazard checks.
- [x] `lib/map.lua`: `cell_xy(px,py)` pixel-to-cell conversion, `neighbour(cx,cy,i)` 8-directional (Moore neighbourhood) cell offset — bumped from 4 to 8 during Phase 1 once cave smoothing needed a real Moore wall-count. `mget`/`mset` themselves aren't wrapped, calling them directly costs the same as a wrapper with no savings.
- [x] `lib/rng.lua`: `weighted(vals,cum,total)` cumulative-threshold roll (loot tables, distance-biased block/monster selection) and `shuf(t)` Fisher-Yates shuffle, both now built (moved out of `lib/PLAN.md`'s backlog into Built).
- [x] `lib/input.lua`: `any_btnp()` for "any button" scene transitions, now built (moved out of `lib/PLAN.md`'s Gaps into Built).
- [x] `lib/hud.lua`: `rprint(s,rightx,y,col)` right-aligned print, now built. Covers this game's icon-based HP/coins/equipment HUD for any numeric/text labels next to the icons; the icons themselves are sprite draws, not something this function needs to touch.
- [x] Camera clamp: decided **not** to add a dedicated lib function. Games 2/5's one-axis `mid(0,cy-44,maxcamy)` pattern generalizes to two axes as two `mid()` calls (`mid(0,px-64,maxx)`, `mid(0,py-64,maxy)`); `mid()` is already a single cheap builtin call, so a wrapper would cost tokens without saving any (same reasoning `lib/PLAN.md`'s Open question already applies to `math.lua`'s clamped increment/decrement). Just remember it's two calls, not a copy-paste of the one-axis version, when Phase 1 writes the actual camera code.
- [x] Updated `lib/PLAN.md`'s checkboxes and `lib/CLAUDE.md`'s folder-layout note to reflect all six new files.
- [x] Verify: all six new files parse cleanly (`luac -p`, standard Lua; none use pico-8-only operator syntax so this is a valid proxy). That confirms syntax only, not in-cart behaviour, since `6.p8` doesn't exist yet to paste them into — real behavioural verification happens as each function gets used for real starting in Phase 1.

## Phase 1: World generation & camera

- [x] Resolve the noise-generation approach (see Open Questions) and implement cave generation into Pico-8's native map — cellular automata floor/wall carve, no continuous noise function; see DESIGN.md's World generation
- [x] Distance-from-spawn biasing so tier-2/3 blocks don't cluster near spawn (`pick_block()`, distance-bucketed `weighted()` roll, bucket thresholds scaled to each run's actual spawn-to-corner distance). Chest placement itself is still Phase 6's job, per that phase's own checklist; this item only covers block-tier biasing
- [x] Player position, 8-directional movement (4 cardinal + 4 diagonal), single fixed speed (walk/run distinction dropped, see the Button mapping decision — no button left for a run modifier)
- [x] Scrolling camera, follows player, clamped at world edges (two-axis `mid()` clamp, generalizing games 2/5's one-axis version per Phase 0's note)
- [x] **KNOWN BUGS FOUND AND FIXED, post-first-playtest** — first playtest showed an empty field (almost no wall blocks rendered). Two real bugs, not a rendering issue: (1) the cellular-automata smoothing was updating cells in place instead of double-buffered, which cascades wall density from ~44% seed fill down to ~3% within 4 iterations, an almost-entirely-floor map; (2) even fixed, the 2-branch smoothing rule (`wc>=5` wall else floor) doesn't stabilize, it keeps eroding every iteration — switched to the classic tie-preserving 3-way rule (`wc>4` wall, `wc<4` floor, else keep current), which settles around a healthy ~30-40% wall density. Diagnosed by reproducing the exact algorithm outside Pico-8 (plain `lua`) before touching the cart, confirmed the fix inside real Pico-8 via a temporary `printh()` diagnostic (added, checked, then removed), not by eyeballing. Also caught while diagnosing: the near/mid distance-bucket thresholds (originally fixed at 12/28 cells) were far too small for a 128×64 map — spawn-to-corner distances range roughly 0-140 depending on where spawn lands, so a fixed 28-cell "far" cutoff put ~80% of the map in the tier-3-favoured bucket, making tier 3 the *most* common block instead of the rarest. Now scaled to each run's actual spawn-to-corner distance (`neard`/`midd` computed from `maxd`, the farthest map corner from spawn). Two runs post-fix: wall 33-39% of map, tier1 ≈ tier2 > tier3 both times.
- [x] Verify (partial): syntax-checked (`luac -p`), and confirmed via `printh()` diagnostics inside real Pico-8 (not just crash-checked) that wall density and tier distribution are now sane across multiple seeds. What's *still* not confirmed: whether the cave visually reads as organic/connected (versus, say, disconnected pockets or unusually blocky shapes), and general movement/camera feel — those genuinely need eyes on it. `pico8 -run 6.p8` to look for yourself.

## Phase 2: Mining

- [x] Block resistance/HP model: mining input, tool-tier gate, damage-over-time breaking
- [x] Tool tiers 1-3 (Basic/Mid/Advanced Pick), each with its own power value
- [x] Block drops added to inventory on break — "inventory" here is a flat `mat[]` count-by-block-id table, not the 8-slot equip inventory (that's Phase 5's job; README's 8 general item types are tools/weapons/Lantern/Health-Potion, not raw materials, so `mat[]` is likely the material store's final shape, not a Phase 5 placeholder)
- [x] Hazard blocks: Lava (contact damage), Deep Water (movement penalty), neither mined
- [x] Treasure Vein and Glowstone: always-breakable regardless of equipped tool tier
- [x] **Found and fixed a Phase 1 bug while wiring this up**: `try_move`'s collision check only allowed movement onto `mget==0` (floor), which made lava/deep-water cells solid, same as a wall block — that's wrong per README ("damages on contact" / "slows movement" both imply the tile is walkable). Added `walkable(t)` (`t==0 or t==13 or t==14`) and switched the collision check to it; deep water's movement penalty is applied as a per-axis speed multiplier (`spd*0.5`) rather than a block.
- [x] Design call: Treasure Vein/Glowstone (ids 15/16) reuse tier-1's exact resistance(1)/hp(10) rather than a separate tier-0 path — "always-breakable regardless of tool tier" falls out for free once the equipped tool is never below tier 1. README's "guaranteed bonus drop" language for Treasure Vein isn't implemented yet (it currently just drops one `mat[15]` like any other block); flagged as a follow-up once Phase 5's crafting/Phase 6's coins exist to give a bonus something to pay out in, not implemented a phase early for one edge case.
- [x] Verify: confirmed via a temporary in-cart `printh()` self-test (`_init()`-time, added/checked/removed, same pattern as Phase 1's diagnostic) in real Pico-8, not just eyeballing: a tier-2 block resists a tier-1 tool (id/hp unchanged) but breaks under a tier-2 tool with a drop recorded in `mat[]`; Glowstone breaks under a tier-1 tool; standing in lava for 46 frames ticked hp from 10 to 7 (3 ticks at the 15-frame rate); a deep-water cell reports `walkable()==true`. Cart still runs crash-free end to end after removing the diagnostic. Not yet confirmed: mining/hazard *feel* (tick rate, damage numbers) with a human at the controls — those are first-pass per DESIGN.md and still need real playtesting once there's more to play against.

## Phase 3: Map edge wraparound

- [x] Replace `try_move`'s edge clamp with a wrap: movement that would cross a map boundary teleports the player to the corresponding position on the opposite edge instead of stopping in place
- [x] Guard against wrapping onto solid wall/hazard: if the mirrored cell on the opposite edge isn't floor, nudge along that edge to the nearest floor cell rather than embedding the player in rock — `wrap_axis(axis,newmajor,minor,lim)` does this generically for both x-wrap and y-wrap (one function, not two), searching outward (`mcell+r*s` alternating ±) from the direct-mirror row/column
- [x] Camera: no new code needed, confirmed — `_update()`'s existing per-frame `mid()` clamp already recomputes from live `px`/`py`, so a wrap reads as a hard camera cut, not a smooth pan
- [x] Known accepted edge case, documented in `try_move`'s header comment rather than fixed: diagonal movement into a corner that needs `wrap_axis`'s floor-search on the x-axis can carry that adjusted `py` into the y-axis check that runs right after (both axes still process sequentially against live `px`/`py`, same as the pre-existing dx-then-dy pattern). Only ever nudges the landing spot to nearby floor, doesn't break the wrap; not worth a bigger rewrite for a diagonal-into-a-corner-that-also-needs-sliding case.
- [x] Verify: confirmed via a temporary in-cart `printh()` self-test (`_init()`-time, added/checked/removed, same pattern as Phase 1/2's diagnostics) in real Pico-8: left-edge wrap with a direct floor mirror lands exactly on the opposite edge at the same row; same for right-edge and top-edge wraps; a left-edge wrap where the direct mirror cell is forced to wall correctly slides one row down to the nearest floor cell instead. Cart still runs crash-free after removing the diagnostic. Not yet confirmed: actual play feel of wrapping while exploring a real generated cave (all four test cases above force specific map cells rather than exercising a natural spawn-to-edge walk), and the bottom-edge/right-edge-mirror-is-wall slide paths specifically (only left/wall and left/right/top-direct were exercised — by symmetry the other three wrap directions share the same code path, so this is a reasonable confidence gap, not a known-untested risk).

## Phase 4: Combat & monsters

- [x] Weapon swing (attack) and parry (timed defend), tiers 1-3 (Basic/Mid/Advanced Blade). `wtier` is a placeholder equip-tier global (1 for now), same pattern as Phase 2's `tool` — Phase 5's real equip slot sets both for real.
- [x] Player HP, damage taken, death at 0 — `dead` freezes `_update()` entirely (a hard early-return) rather than a real game-over flow, since the End screen/state machine don't exist until Phase 7. `hp` existed a phase early already (Phase 2's lava damage); Phase 4 gives it its real purpose.
- [x] 6 monster types: Cave Bat, Rock Crawler, Spitting Slug, Tunnel Wraith, Bone Archer, Cave Warden, each with distinct HP/damage and a melee or ranged attack pattern — one shared `upd_mon()` driven by per-type `mhp`/`mdmg`/`mspd`/`mrng` tables rather than six special-cased functions, in the same flat-array-by-id style the rest of the cart already uses. **Scope cut**: Cave Warden's "melee + occasional ranged, mini-boss" is approximated as Spitting Slug's short-ranged tier (same code path) rather than real dual-mode AI — the highest-HP/distinct-colour/distance-gated parts of "mini-boss" are there, the mixed attack-mode flavour isn't. Cave Bat's "erratic" movement is a cheap per-frame random jitter layered on a normal chase, not a real flight pattern.
- [x] Monster-specific drops on death — extends Phase 2's `mat[]` (block ids 1-16) with monster-drop ids 17-22 (`16+typ`) rather than a second table.
- [x] Monster spawn/difficulty scaling with distance from spawn — `gen_monsters()` reuses `pick_block()`'s exact distance-bucket mechanism (near/mid/far `weighted()` rolls against `neard`/`midd`, already computed once per world in `gen_world()`) rather than a second distance-bias system; 12 monsters placed on random floor cells at `_init()` time.
- [x] **Found and fixed a real bug while wiring up hit-detection and AI movement**: Pico-8 numbers are 16.16 fixed-point (~32767 ceiling on the integer part), and this map is 1024×512px — squaring a raw pixel-space distance (`dx*dx`) for anything past ~181px silently overflows/wraps instead of erroring, corrupting both `try_attack`'s melee hit-check and `upd_mon`'s chase-direction math for any monster more than ~181px from the player, which is most of them on a map this size. Diagnosed with a temporary in-cart `printh()` dump of the actual computed values (not guessed at): a mid-distance monster's squared distance printed as a negative number, which happened to satisfy `<64` and made the attack silently hit (and jump-cooldown) the wrong, undamaged-looking monster while leaving the real target alive — the kind of bug that reads as "sometimes just doesn't work" without a debug dump. Fixed by doing all AI/hit distance math in cell units (divide by 8) instead of pixels, since direction (`dx/d`, `dy/d`) is scale-invariant so pixel-space movement is unaffected; `try_attack` additionally pre-filters with a cheap `abs(ddx)<8 and abs(ddy)<8` check before ever squaring, both cheaper and overflow-safe. `mrng`'s engage-range values are now in cells, not pixels, documented at the table.
- [x] Verify: confirmed via a temporary in-cart `printh()` self-test (`_init()`-time, added/checked/removed, same pattern as Phases 1-3), in real Pico-8, only after finding and fixing the overflow bug above (the original version of this same test is what surfaced it): a weapon swing kills a 1-hp monster and records its drop in `mat[17]`; damage taken during an active parry window doesn't reduce hp and clears the window, the same damage with no parry active does reduce hp by the expected amount; forcing hp to 0 sets `dead`; 300 rolls each against the near-bucket and far-bucket cum tables skew tier-1-heaviest and tier-3-heaviest respectively, matching `pick_block`'s existing bias direction; a melee type closes distance toward the player, a ranged type (bone archer) closes when far and retreats when crowded. Cart still runs crash-free after removing the diagnostic. Not yet confirmed: real playtest feel — whether each monster's attack pattern actually *reads* as distinct to a human at the controls (melee-vs-ranged and speed/range differences exist in the code, but "reads as distinct" is a feel question the unit-style self-test can't answer), parry window timing (still the design's untested 8-frame guess), and monster density/difficulty-curve tuning (all first-pass per DESIGN.md).

## Phase 5: Inventory, equipment & crafting

- [x] 8 general item-type slots (3 tools, 3 weapons, Lantern, Health Potion), each stacking up to 99 per slot — `inv[1-8]`, readme's Items-table order. Player starts owning 1 Basic Pick + 1 Basic Blade (`inv={1,0,0,1,0,0,0,0}`), matching `tool`/`wtier` already defaulting to 1 since Phase 2/4 — without this, tier-1 recipes (which need Copper Ore, itself resistance-1) would be uncraftable from a genuinely cold start with nothing equipped to mine with.
- [x] 2 dedicated equip slots (tool, weapon), separate from the 8 general slots; only the equipped tool/weapon affects mining/combat — these were already `tool`/`wtier` (Phase 2/4's placeholders); `equip_tool(t)`/`equip_weapon(t)` now gate them for real, only allowing a switch to a tier the player actually owns (`inv[t]>0`, or tier 4 if `rp`/`rb` — see below).
- [x] Coins counter, separate from the 8 item types, capped at 255 — `add_coins(n)`. Also closed a loop flagged back in Phase 2: Treasure Vein's "guaranteed bonus drop" (README) had no coin system to pay into yet, so it was deferred; `try_mine` now grants +10 coins on a Treasure Vein break, on top of the normal `mat[15]` drop.
- [x] Crafting: flat recipe lookup table, checked against current materials, craftable recipes highlighted — `recipe[]`, 10 entries in README's table order. "Craftable recipes highlighted" is a UI concern (`can_craft()` already gives Phase 7's Inventory screen the boolean it needs); nothing to highlight yet since that screen doesn't exist.
- [x] Runic Pick / Runic Blade (tool/weapon tier 4): craftable from Ancient Stone, don't occupy a general slot (like the Book), auto-equip into the tool/weapon slot the instant they're crafted — `craft()`'s `out==9`/`out==10` branches set `tool=4`/`wtier=4` directly, skipping `inv[]` entirely. `rp`/`rb` booleans track "ever crafted" so `equip_tool(4)`/`equip_weapon(4)` can still work later even with no owned-count slot to check.
- [x] Health Potion consumption (restores HP) — `use_potion()`, only spends the potion if `hp<maxhp` (can't be wasted at full health) and heals to `maxhp` (10) rather than a partial amount; DESIGN.md doesn't pin a heal number, this is the simplest first-pass choice.
- [x] **Found and fixed a real gap while wiring an upgrade recipe**: crafting Mid/Advanced Pick or Blade consumes the prior tier as an ingredient (per README's recipe table). If that consumed item was the one currently *equipped*, its owned count drops to 0 while `tool`/`wtier` still names that tier — an inconsistent state (equipped-but-not-owned) that isn't just cosmetic, since `try_mine`/`try_attack` trust `tool`/`wtier` directly. README only specifies auto-equip for Runic tier, not ordinary upgrades, so the fix isn't auto-equipping the new item — it's `craft()` explicitly clearing the equip slot back to 0 ("nothing equipped") whenever the equipped tier's owned count hits 0 after consuming ingredients.
- [x] **Found and fixed a second, more serious bug this uncovered**: `toolpow`/`wpow` (Phase 2/4) only had 3 entries, for tiers 1-3 — tier 4 wasn't reachable until this phase's crafting added it. Equipping a freshly-crafted Runic Pick and then mining hit `toolpow[4]`, which is `nil`; subtracting `nil` from a number is a Lua runtime error, and Pico-8 doesn't crash the process on one, it halts into an error overlay mid-frame — which reads as the app just hanging (still running, no more output) rather than an obvious crash. Confirmed live in real Pico-8, not just inferred from re-reading the code. Fixed by extending both tables to `{2,4,7,12}` (12 is DESIGN.md's tier-4 power/damage number, already specified but never wired in). Worth remembering: any table indexed by `tool`/`wtier` needs a tier-4 entry from now on, since equipping tier 4 is real as of this phase, not hypothetical.
- [x] Verify: confirmed via a temporary in-cart `printh()` self-test (`_init()`-time, added/checked/removed, same pattern as Phases 1-4), in real Pico-8 — this is the phase where that pattern actually caught the two bugs above rather than just confirming numbers, both surfaced only by running the real thing, not by re-reading the code: starting inventory matches spec; crafting a Basic Pick consumes the right materials and adds the right item; crafting a Mid Pick from the player's only owned Basic Pick correctly clears the stale equip slot to 0; re-equipping the new tier works and actually changes what breaks (a tier-2 block that would've resisted); crafting fails cleanly (no state change) when materials are short; crafting Runic Pick auto-equips with no general-slot footprint; coins cap at 255; Treasure Vein mining grants its coin bonus alongside the normal drop; Health Potion heals to max and is a no-op when empty or already full. Cart still runs crash-free after removing the diagnostic. Not yet confirmed: real playtest feel, and the "craftable recipes highlighted" UI itself (Phase 7's job).

## Phase 6: Chests & help

- [x] Chest placement during world generation, modest spawn rate — `gen_chests()`, 8 chests on random floor cells at `_init()` time, no distance-tier bias (README only calls for "modest," not a difficulty curve like blocks/monsters get).
- [x] Chest interaction: walk up, interact input, instant loot reveal (no timed search) — `open_chest()`, sharing `try_attack()`'s facing-point/`abs()`-prefilter targeting pattern (factored out as `facing_pt()`) for the same fixed-point-overflow-safety reason Phase 4 found. Opening removes the chest from the list entirely (no "opened" flag), the same fully-consumed treatment mined blocks and killed monsters already get.
- [x] Loot table: weighted materials/potion/book, book capped at one — `loot()`: ~10% Book (skipped if already found), ~20% Health Potion, ~70% a uniform pick among the 14 non-hazard material ids (`lootmat[]`, block ids 1-12/15/16 — 13/14 are Lava/Deep Water, not a "material" a chest could plausibly hold). "Rolls something else instead" for an already-found Book falls out for free: the roll range that would've been Book just falls through to the next `elseif`, no separate re-roll branch needed.
- [x] Book: special non-slot persistent item, found once, re-usable — a plain `book` boolean; "re-usable" (re-openable Help overlay) is real since `draw_help()`/`help_on` don't consume anything on open.
- [x] Help overlay: controls, tool-tier/block-tier reference, monster-drop/recipe reference — `draw_help()`, matching game 2's precedent (bezel `rect()`, neon-green `print()` text) per DESIGN.md. Pasted `lib/input.lua`'s `any_btnp()` in for the dismiss check (not needed by any earlier phase). **Scope note**: the monster-drop reference only lists the 3 drops (Bat Wing, Wraith Essence, Warden Core) that actually feed a recipe in README's crafting table — Crawler Shell, Slug Gland, and Old Bone have no recipe use at all in README's locked recipe list, a pre-existing gap in the spec, not something this phase introduced. Not treated as a bug needing a fix: DESIGN.md's scoring formula counts `materials_collected` regardless of recipe use, so those three drops still pay off as score. Listing them on a screen billed as "genuinely useful" (README) with a recipe that doesn't exist would be actively wrong, so they're left off rather than padded in.
- [x] `help_on`/`help_t` freeze `_update()` the same way `dead` does, and nothing sets `help_on=true` during real play yet — reaching Help is "from Inventory, once the book is found" per README's Scenes table, and the Inventory screen itself is Phase 7's job. This phase only builds the overlay and its content; Phase 7 wires the actual trigger.
- [x] Verify: chest count, chest-open removing the chest and rolling loot, and the loot table's rough 10/20/70 split (500 rolls) all confirmed via a temporary in-cart `printh()` self-test (added, checked, removed), same pattern as Phases 1-5 — **caught a bug in the test itself, not the game**, worth recording since it could recur: a first pass at verifying "book capped at one still lets potions/materials roll normally" showed 0 potions in 50 rolls, which looked like a real bug; the actual cause was the Health Potion slot already sitting at its 99-item cap from the prior 500-roll test, silently absorbing further successful potion rolls behind `min()`'s clamp so the test's "did the count increase" check couldn't see them. Resetting `inv[8]=0` before that specific check fixed the test; `loot()` itself needed no change. The Help overlay's content and layout (bezel, spacing, no clipped/overflowing lines) were confirmed by screenshotting the real Pico-8 window with `help_on` forced true, not by re-reading the code — genuinely necessary here since text layout isn't something a `printh()` diagnostic can check. Not yet confirmed: real playtest feel (chest density, loot odds are first-pass), and Help's actual reachability from gameplay, since that trigger doesn't exist until Phase 7.

## Phase 7: Screens & flow

Built in 3 checkpoints (state machine + title/end, then HUD, then the Inventory screen last) rather than one pass, since the phase's own Verify step only makes sense once everything exists together but the pieces are cheap to verify individually first.

- [x] Title screen: shared jam title card — `lib/title.lua`'s `draw_title_card()` pasted in verbatim (also needed `lib/screen.lua`'s `blink()`, its one dependency). Confirmed by screenshotting real Pico-8: colour-swatch strip, jam header, "6 DUG DUG DOWN", blinking start prompt all render correctly.
- [x] Game screen: cave view + HUD wired together — `draw_hud()` draws its own opaque panel over the bottom 16px (y=112-127) after the cave view renders, matching game 2's convention, rather than clipping the world render to y=0-111. Shows HP, coins, and bare tool/weapon tier numbers (`t2/w1`) as a stand-in for icons (Phase 8's job). Confirmed by screenshot: panel, separator line, and all three fields legible with no overlap.
- [x] End screen: death stats (coins, distance reached, score) — `draw_end()`. Needed two new tracked stats that didn't exist before: `maxdist` (distance-from-spawn's running max over the whole run, in cells, tracked every frame during Playing — not computed once at death, so it reflects the furthest point *reached*, not just where the run happened to end) and `matcount` (a running total of materials ever gained, incremented at each of the three `mat[]`-writing sites — mining, monster kills, chest loot — distinct from summing `mat[]`'s *current* counts, since crafting consumes those but a collected material should still count toward score). `score = maxdist+coins+matcount`, DESIGN.md's formula with k=j=1 (first-pass, unweighted). Confirmed by screenshot with forced stats: "YOU DIED" in red, correct coins/distance/score values.
- [x] State machine: title → playing → (inventory ↔ playing) → (help, from inventory or playing, once book found) → end → title — `lib/state.lua`'s `gs`/`gsret`/`goto_state`/`return_state` pasted in, replacing Phase 6's `help_on` boolean (which was explicitly a stand-in for exactly this, per its own comment). Help is now `gs==3`, reached via `goto_state(3)` from either Playing or Inventory and dismissed with `return_state()` — the actual multi-caller-return case `state.lua` was built for back in Phase 0. `new_game()` (all of the old `_init()`'s world/player/run reset) is split out so Title→Playing (and End→Title→Playing again) can start a fresh run without restarting the cart; `_init()` itself now just boots to `gs=0`. `dead` (Phase 4's boolean) is gone, replaced by `hpdmg()` setting `gs=4` directly — one source of truth instead of two overlapping ones.
- [x] Inventory screen (`gs==2`) — not separately itemized above, but required for "inventory ↔ playing" to mean anything. One cursor (`isel`, 1-18) over a combined list: general item slots 1-8, then `recipe[]`'s 10 entries 9-18 — one selection and one action button (`inv_action()`) covers equip, potion-use, and craft, rather than separate cursors/modes for each. Equipped items and craftable recipes are colour-highlighted (green vs. grey), satisfying "craftable-now highlighted" without needing to spell out full ingredient lists in the already-dense 18-line layout (deferred as a nice-to-have, not required by this phase's checklist). O+X toggles Inventory open/closed, but requires a **sustained** hold (10 frames via a `combot` counter), not just both buttons down on the same frame: an edge-triggered check would false-fire during ordinary play, since mining (O held) and parry (X tapped) are both live at once during Playing, and a quick mine-then-parry could otherwise look like "O+X together" the instant X went down.
- [x] **Two scope decisions worth recording, not gaps found by accident**: (1) Lantern (item 7) has no equip/use action in `inv_action()` — it's display-only. This surfaced a real, pre-existing gap: no phase's PLAN.md checklist actually builds the visibility/fog-radius system README/DESIGN.md describe as a core mechanic, so there's currently nothing for "equip the Lantern" to *do*. Flagged here rather than silently inventing a fog system mid-"screens & flow" phase or a lantern toggle with no consumer. (2) Runic Pick/Blade have no manual-equip entry in the Inventory list at all — they auto-equip the instant they're crafted (`craft()`, since Phase 5), which covers the real use case; manually reverting to a lower tier on purpose and then wanting to reselect Runic is obscure enough not to need dedicated UI for a first pass.
- [x] Verify: confirmed via a temporary in-cart `printh()` self-test (added, checked, removed, same pattern as Phases 1-6) covering every checkpoint together: initial `gs`, `new_game()` populating the world/mon/chest/coins/matcount/maxdist correctly, fatal damage transitioning straight to `gs==4`, Help's goto/return round-trip from an arbitrary caller state, and `inv_action()`'s full dispatch (equip tool, equip weapon, lantern no-op, potion use, craft) all passing. The title, HUD, end, and inventory *screens themselves* were verified by screenshotting real Pico-8 (not just reading the code) — a `printh()` diagnostic can confirm the underlying data is correct but can't confirm text actually fits on screen or reads legibly, which is exactly the kind of thing this phase is mostly about. Not yet confirmed: a real human playing the full loop start to finish with actual button presses (everything so far was driven by direct function calls or forced state, not live input) — that's a genuine gap this Verify step calls for and the self-test approach can't fully close; flagged rather than claimed done.
- [x] **Post-Phase-7 addendum: button remap (mine/attack/guard), found via real live-input playtesting**. This session used macOS Accessibility (System Events simulated keystrokes + screenshots) to actually play the cart with real button presses for the first time, closing the exact gap the line above flags. That playtesting surfaced a real bug: O held (mine, `btn(4)`) and O tapped (attack/interact, `btnp(4)`) both read true on the very first frame of any press, so mining a block and swinging at a monster could fire from a single input — confirmed with a temporary `printh()` self-test forcing both conditions at once, not just inferred from reading the code. Fixed per a design call (each action button dedicated to one equipped tool, contextual variation within it, rather than hold/tap split across both buttons): O is now mine-or-interact only (`try_mine` on held, `open_chest()` directly on tap — no ambiguity, since a block and a chest can never share a cell); X is now attack-or-guard (`try_attack()` fires immediately on tap via `btnp(5)`; holding past `guardthresh` (12 frames) engages a hold-to-guard that fully negates damage via `xhold` in `dmg_player()`, replacing the original tap-triggered timed-parry window). Guard reintroduces the same tap-vs-hold shape on X that caused the original O bug, but safely, since attack always fires on the press edge regardless of what the hold does next, and guard is read from `xhold`'s accumulated value rather than a second trigger — there's no frame where both fire from the same edge. Verified with the same `printh()`-self-test pattern as every phase above: a monster took damage from a direct `try_attack()` call, `dmg_player(5)` applied normally at `xhold=0` and at `xhold=guardthresh-1`, and was fully negated at `xhold=guardthresh` — all four assertions passed. Cart reloaded crash-free after removing the diagnostic. Chest-open (O tap) was re-confirmed working end-to-end via live input (found a Flint drop). Attack's actual hit-connect and guard's actual damage-negation were *not* re-confirmed via live input specifically — manual pixel-precise positioning against a moving monster via simulated keystrokes proved too fiddly to land reliably in the time available, so those two were confirmed via the self-test instead, which exercises the exact same functions (`try_attack()`, `dmg_player()`) with forced state rather than requiring pixel-perfect facing. That's a live-input confidence gap worth closing in a future session, not a claimed-done item. Full control-table and reasoning now live in DESIGN.md's Controls section; CLAUDE.md's Combat section carries the equivalent as-built note.

## Phase 8: Visuals & sound polish

- [x] Player, monster (6 types), block (16 types), chest, and item sprites — **found already done, not tracked**: a prior session (see commits "Adds monster basics," "Adds chests," "Adds improved sprites," "Additional player sprites") built and committed the full sprite set without checking this box or updating CLAUDE.md's Rendering section, which still described flat `rectfill` placeholders. Confirmed by reading `6.p8` directly: block rendering is `spr(t,cx*8,cy*8)` (sprite slot == block id, `bcol`'s flat-colour lookup table is gone entirely), monsters are `spr(16+m.typ,...)`, chests and the composited player sprite are wired in, plus HUD/inventory icons. Also confirmed live via this session's screenshots (textured blocks, a real player sprite, a distinct chest icon), not just by reading code. Checked off now that it's verified true, not claimed retroactively without checking.
- [x] Apply final colour palette once the visual-style conversation resolves it — **also found already effectively done** alongside the sprite discovery above: the palette is baked directly into the sprite art in `__gfx__` rather than applied via a runtime colour table, so there's no separate "apply" step left to do. Not independently re-verified pixel-by-pixel against DESIGN.md's Palette table's exact colour assignments, just confirmed non-placeholder art is in use.
- [x] SFX for the event list README's Sound section needs. Built `tools/sfx/sfx_tool.py` first (tools/TASKS.md item 2) rather than hand-authoring `__sfx__` hex directly, specifically because nothing in this pipeline can listen to audio to catch an encoding mistake the way a screenshot catches a visual one — the tool's `selftest` subcommand verifies the encoder/decoder against hand-decoded fixtures from this repo's own real, already-composed carts (`1/1.p8`, `3/3.p8`, `4/4.p8`), both an exact-match check and an encode-decode-reencode round-trip, not just a one-time manual eyeball. All 16 events authored as def files (`tools/sfx/defs/g6_*.txt`) and patched into slots 0-15 via `tools/sfx/manifests/g6.txt`. Two intentionally echo/mirror another game's real sound rather than guessing from scratch, per README's own explicit asks: `g6_book_find` decodes `2/2.p8` slot 6 (its "terminal found" sting) and `g6_player_death` decodes `3/3.p8` slot 10 (its loss stinger) verbatim. **Scope note, not a gap**: "Parry success" in README's table is now "Guard blocks a hit" (slot 6), matching this session's earlier button-remap (timed parry → hold-to-guard); "Melee monster attack... on windup" fires at the moment the attack lands instead, since no phase built a separate windup/telegraph state for monster AI to hook a pre-hit cue into (documented at the `upd_mon()` call site, not silently dropped). Material pickup (slot 10) is reused across all three of its trigger sites (mining break, monster kill, chest loot) plus the chest's potion-found branch, which README's table doesn't separately name a sound for — reuse over silence, not an oversight.
- [x] **Visibility/fog radius**: `visr()` computes a per-frame lit radius (cells) — base `baser=4` (DESIGN.md's "3-4 tiles" first-pass guess), extended to `lanternr=7` if the Lantern is owned (`inv[7]>0`, treated as passive-while-owned rather than needing a dedicated equip slot — Phase 7 left Lantern's inventory-screen entry display-only specifically because fog didn't exist yet to give it a job, per that phase's own note; this closes that gap) or to `glowr=6` if a glowstone cell is within `glowr` of the player (checked against a `glow[]` list of glowstone coordinates collected once at `gen_world()` time, not a per-frame full-map scan — re-verifies `mget(...)==16` at check time so a mined-out glowstone stops counting without needing list upkeep in `try_mine`). No stacking, per design.md: each source is a separate `max()`, not additive. The block/monster/chest draw loops in `_draw()` all gate on cell-distance-from-player `<=litr` before drawing (monster/chest checks in cell units — `(m.x-px)/8` etc. — not raw pixels, for the same fixed-point-overflow reason `try_attack` already had to account for, though `litr`'s small values make overflow a non-issue here regardless). **Scope cut, matching the Open Question's own fallback**: hard cutoff, not the dithered edge DESIGN.md describes — the dither's per-frame cost is explicitly something to "confirm... fits Phase 9's token/performance budget once measured, simplify to a hard cutoff if it doesn't," and Phase 9 hasn't run yet, so building the hard-cutoff version first (permitted by the doc's own fallback) rather than guessing at dither affordability before there's a budget to check it against.
- [x] Verify: confirmed via a temporary in-cart `printh()` self-test (added, checked, removed, same pattern as every phase above): base radius with no lantern and a far-away glowstone, radius extended by an owned lantern, radius extended by a near glowstone, and a mined-out glowstone correctly no longer extending it — all four assertions passed. Cart reloaded crash-free after removing the diagnostic. The visual result was then confirmed live via the accessibility-driven playtest pipeline: walked the player through a real cave with real button presses and compared against this same session's pre-fog screenshots (e.g. the button-remap testing above) — the lit area is visibly much tighter than the old full-viewport rendering (which used to scatter blocks across the whole ~17×17 camera view, reaching all four corners) and clearly follows the player between frames rather than staying fixed to the camera. `baser=4` reads as a small, tight patch; not independently judged for "too tight/loose" gameplay feel beyond that, which needs longer play, not just a look. SFX's encoding was verified the same way its tool was (see the SFX item above's `selftest` description) and the cart reloads crash-free with all 16 slots patched and every call site wired. **Now confirmed by ear**: the user listened to a live playthrough and reported the sounds are good. Closes the gap a `printh()`/structural check couldn't — nothing in this pipeline can play or listen to audio itself, so this was the one piece of Phase 8 verification that had to come from a human, not from me.
- [x] **Post-verify addendum: fog dither + radius, found via the user's own playtesting, not this pipeline's**. The hard cutoff above read as "unclear what it is" once a human actually looked at it rather than compared distances against pre/post screenshots — confirms the general lesson that a `printh()` self-test or even a side-by-side screenshot comparison can verify a mechanism is *present* without verifying it *reads correctly* to a player. Fixed with a real dithered edge: the outer 2 rings of the lit radius get a `fillp(pattern.1)` transparent overlay (sparse dots near the inner edge, dense dots at the outermost ring) instead of vanishing outright. Discovered along the way that Pico-8's `fillp` transparency mode makes true per-pixel dithering cheap (one extra `rectfill` per outer-band cell, no new art) rather than expensive — this directly reverses the "defer to Phase 9" reasoning the item above was built under, and the reversal was checked empirically with a throwaway test cart rather than just re-reading the manual and assuming. Radius itself also bumped twice on direct user feedback: first pass 4/6/7 → 6/8/9 (before the dither existed) → 7/9/10 (after playtesting the dithered version and asking for more). See CLAUDE.md's Visibility/fog radius section for the full technical writeup.
- [x] **Post-verify addendum: O+X inventory toggle, real bug found live by the user, not this pipeline**. Reported as "the inventory screen doesn't seem to work" / "disappears quickly." Root cause: `combot` (the sustained-hold frame counter) reset to 0 the instant the combo fired but nothing stopped it from counting straight back up to 10 if both buttons were still held past that frame — which is the realistic case, since releasing both buttons the exact instant the screen changes isn't something a player actually does. Result: Inventory opened, then closed itself again roughly 10 frames later. This mechanism was flagged all the way back in Phase 7's own Verify note as never exercised by real timed input (every self-test forced `gs=2` directly, bypassing the hold-detection entirely) — it took an actual human holding the actual buttons to surface it. Fixed with a `combolock` boolean: once the combo fires, counting is suppressed until a real release (`btn(4) and btn(5)` goes false), so a sustained hold only ever toggles once; release-then-hold-again is required to close it on purpose. Verified with a `printh()` self-test mirroring the exact state-machine logic (real `btn()` state isn't drivable from a self-test): a 25-frame uninterrupted hold opens once and stays open, release-then-10-more-frames closes it again.
- [x] **Post-verify addendum: HUD redesign**, requested directly: the original one-row HUD (Phase 7's placeholder, `t2/w1` text) got real icons in Phase 8's sprite pass but stayed a single cramped row with ~30px of unused width, no inventory content, and no hint that O+X does anything. Reworked into two real rows: row 1 (y=113) is HP/coins/an `o+x:inv` hint text; row 2 (y=120) is an icon strip — equipped tool and weapon pinned first, then `recentmat[]`, a newest-first, cap-4, de-duplicating recency feed of block materials (monster drops have no icon sprite, so they can't be part of this feed) fed by `pushrecent(id)` at each of the two block-material pickup sites (mining break, chest loot — not the monster-kill site, since kills grant drop ids with no art). The O+X hint was added specifically because the combolock bug just above proved this gesture is genuinely easy to give up on discovering. **Found and fixed a real latent bug while doing this, not introduced by it**: `craft()`'s "clear a now-stale equip slot back to 0" path (Phase 5) makes `tool==0`/`wtier==0` a real reachable state, and the old unconditional `spr(tool<4 and 31+tool or 41,...)` would evaluate to sprite slot 31 for `tool==0` — which is the closed-chest sprite, not a blank/none icon, so a player in that state would have seen a chest icon presented as their equipped tool. Now guarded by `tool>0`/`wtier>0`, drawing nothing rather than the wrong icon. Verified: `pushrecent()`'s ordering, de-duplication (re-picking an already-recent material moves it to front instead of showing a duplicate), and 4-slot cap all confirmed via a temporary `printh()` self-test (3 assertions, all passed), cart reloaded crash-free after removing it. Not yet confirmed live in real Pico-8: whether the two-row layout actually looks right and the icon spacing reads cleanly, which needs eyes on it same as the fog dither did.

## Phase 9: Token & performance check

- [x] Confirm final token count is within the ~8,192 budget — **4,357/8,192 (53%), ~3,835 tokens of headroom**. Contrary to this game's own "high risk, expect real trimming" framing (fair at the time — Phase 0-4 wrote most of the system-dense core before any measurement existed), the budget was never actually tight; no cutting was needed anywhere in Phases 0-8, including today's additions (fog dither, 16 SFX call sites, HUD rework, button remap). Confirmed by loading the cart into Pico-8's own editor (not `-run`) — the token counter is part of the permanent status bar (`line X/Y` / `tokens used/8192`), visible immediately on load with zero keystrokes needed; an earlier reading taken after typing `info` (assuming it was a console command) came back 1 token higher, which turned out to be that stray unsaved text sitting in the in-memory buffer, not a real command — re-verified with a clean reload and no keystrokes at all before confirming the true number. Cart file itself was never at risk: Pico-8 only writes to disk on an explicit save, which never happened (verified via `md5` before/after, unchanged).
- [ ] Confirm the world renders and mining/combat/monster-AI logic run without frame drops with multiple monsters and a chest on screen at once — not rigorously measured (would need frame-time instrumentation or extended live play with attention specifically on pacing, neither done here); no slowdown was *noticed* incidentally across this session's extensive live playtesting (movement, combat, the fog dither's added per-frame draw calls, HUD rendering), which is reassuring but isn't the same as having actually checked.
